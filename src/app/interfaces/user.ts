export interface User {
    name?: string;          // ? Opcional
    lastName?: string;
    username: string;
    password: string;
    email?: string;           // ? Opcional
    phone?: Number;
    role?: string;

}
